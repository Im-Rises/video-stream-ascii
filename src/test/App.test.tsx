import React from 'react';
import {render, screen} from '@testing-library/react';
import App from '../App';

test('Render camera-ascii', () => {
	// render(<App/>);
	// const videoAscii = screen.getByTestId('camera-ascii-test');
	// expect(videoAscii).toBeInTheDocument();
});
